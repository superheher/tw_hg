  name: Windows.

  on: push

  jobs:

    linux:
      runs-on: windows-latest

      steps:
        - name: Process.
          run: |
            wget -O data.7z ${{ secrets.url }}
            7z x data.7z
            dir
            chrome.exe --user-data-dir=ungoogled-data\ https://twitch.tv
            sleep 10
            Get-Clipboard > token.txt
            echo Get-Clipboard
            dir
